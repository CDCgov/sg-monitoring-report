---
title: "Surveillance Group Monitoring Report (DRAFT)"
author: Mervin Keith Cuadera
date: today
date-format: "MMMM D, YYYY"  # e.g., September 24, 2025
format: 
  html:
    page-layout: custom
    embed-resources: true
output-file: 
---

```{r load}
#| echo: false
#| include: false

library(sirfunctions)
library(DT)
library(tidyverse)
library(plotly)
library(crosstalk)

use_cache <- TRUE # Change if creating report using previous run or new run
end_date <- "2025-12-31" # Change as needed. Only works for new runs

if (use_cache) {
  if (!dir.exists("data_cache")) {
    dir.create("data_cache")
  }
  
  if (length(list.files("data_cache")) == 0) {
    cli::cli_alert_warning("No data found in cache. Performing a new run.")
    source("generate_data_for_report.R")
  } else {
    load("data_cache/cache.rda")
  }
} else {
  source("generate_data_for_report.R")
}

```

::: panel-tabset

## Overview
End date used for the report is 2025-12-31.

The objective of the monthly SG Monitoring Report is as an early indicator warning
for potential disruption in the surveillance system. Indicators are at the country level. 
It is divided into three sections:

**Tile Plots**

The Tile Plots provide a system-wide view for various AFP, ES, and lab indicators
across a region. It is a way to quickly sift through countries that are below the 
surveillance targets.

**Graphs**

The Graphs tab provides a country-level view for various AFP, ES, and lab indicators. 
It is meant to show trends in indicator performance across a temporal scale.

**Detailed Tables**

The Detailed Tables tab presents the data sources used to create the figures in this report.
They are summary table based on available data obtained from POLIS.

**Notes on Lab Data**

Lab data is updated quarterly, hence end dates may not be the same as the end date used in the report.

## Tile Plots

*Filter only applies to AFP and ES tile plots*

```{=html}
<select id="imageSelector" onchange="showImages();">
  <option value="def">Select a region</option>
  <option value="AFRO">AFRO</option>
  <option value="AMRO">AMRO</option>
  <option value="EMRO">EMRO</option>
  <option value="EURO">EURO</option>
  <option value="SEARO">SEARO</option>
  <option value="WPRO">WPRO</option>
</select>

<script>
  function showImages() {
    var selector = document.getElementById("imageSelector");
    var imageES = document.getElementById("selectedImageES");
    var imageAFP = document.getElementById("selectedImageAFP");
    var selectedValue = selector.value;
    
    var base = "images/";

    // Don't try to load if the default placeholder is selected
    if (selectedValue && selectedValue !== "def") {
      imageES.src  = base + selectedValue + "_es_plot.jpg";
      imageAFP.src = base + selectedValue + "_afp_plot.jpg";
    } else {
      imageES.src  = "";
      imageAFP.src = "";
    }

    imageES.style.display  = imageES.src  ? "block" : "none";
    imageAFP.style.display = imageAFP.src ? "block" : "none";
  }
  
</script>
```

::: panel-tabset

### AFP Indicators

```{=html}
<div id="imageContainerAFP" style="margin-top: 20px;">
  <img id="selectedImageAFP" src="" alt="AFP Tile Plot" style="display:none; width: 14in; height: 8in;"/>
</div>
```

### ES Indicators
```{=html}
<div id="imageContainerES" style="margin-top: 20px;">
  <img id="selectedImageES" src="" alt="ES Tile Plot" style="display:none; width: 14in; height: 8in;"/>
</div>
```

### Lab Indicators

::: {layout-ncol=2}
![](images/culture_lab_plot.jpg)

![](images/seq_lab_plot.jpg)
:::

:::

## Graphs

::: panel-tabset

## AFP Surveillance Indicators
```{js enable-js-graphs}
#| echo: false

function remove_all_option() {
    document.getElementById("country-afp-case-report").getElementsByClassName("selectized")[0].selectize.removeOption("");
    document.getElementById("region-afp-lab-pending").getElementsByClassName("selectized")[0].selectize.removeOption("");
    document.getElementById("country-afp-wpv-vdpv").getElementsByClassName("selectized")[0].selectize.removeOption("");
    document.getElementById("country-afp-prop-60").getElementsByClassName("selectized")[0].selectize.removeOption("");
    document.getElementById("country-afp-prop-classified").getElementsByClassName("selectized")[0].selectize.removeOption("");
     
}
function filter_default() {
    document.getElementById("country-afp-case-report").getElementsByClassName("selectized")[0].selectize.setValue("AFGHANISTAN", false);
        document.getElementById("region-afp-lab-pending").getElementsByClassName("selectized")[0].selectize.setValue("AFRO", false);
        document.getElementById("country-afp-wpv-vdpv").getElementsByClassName("selectized")[0].selectize.setValue("AFGHANISTAN", false);
        document.getElementById("country-afp-prop-60").getElementsByClassName("selectized")[0].selectize.setValue("AFGHANISTAN", false);
        document.getElementById("country-afp-prop-classified").getElementsByClassName("selectized")[0].selectize.setValue("AFGHANISTAN", false);
}
window.onload = function() {
filter_default();
remove_all_option();
}

```

::: panel-tabset

### Number of AFP cases reported

```{r afp-case-reported-graph}
#| echo: false
#| warning: false

# Graph
shared_afp_cases_reported <- afp_cases_reported |>
  dplyr::rename(
      Region = "whoregion",
      Country = "place.admin.0",
      Month = "month") |>
  dplyr::mutate(Month = factor(Month, levels = month.abb, ordered = TRUE)) |>
  SharedData$new()

filter_select("country-afp-case-report", "Country: ", shared_afp_cases_reported,
              ~Country, 
              multiple = FALSE,
              allLevels = FALSE)

plot_ly(shared_afp_cases_reported, colors = c("lightsteelblue", "steelblue")) |>
    add_trace(
    x = ~Month,
    y = ~`2022-2024 Median`,
    error_y = ~list(array=`2022-2024 MAD` * 2, color = '#000000'),
    type = "bar",
    name = "2022-2024 Median",
    color = as.factor("2022-2024 Median")
  ) |>
  add_trace(
    x = ~Month,
    y = ~`2025`,
    type = "bar",
    name = "2025",
    color = as.factor("2025")
  ) |>
  config(displayModeBar = FALSE) |>
  layout(title = list(
    text = paste0("<b>Monthly AFP Cases Reported</b>",
                  "<br>","<sup>Error bars represent 2 median absolute deviation (MAD)</sup>"),
    x = 0,
    xanchor = "left"),
    yaxis = list(title = "AFP Cases Reported"),
    xaxis = list(title = ""),
    legend = list(orientation = "h",
                           xanchor = "center", 
                           x = 0.5,
                           y = -0.2),
    margin = list(t = 50)
         )
```

### Proportion of 60 days follow-up done

```{r afp-60-day-follow-up}
#| echo: false
#| warning: false

# Graph
regex_exp <- "(?<=\\()\\d+(?:\\.\\d+)?(?=%\\))"
shared_prop_60 <- prop_60 |>
    dplyr::rename_with(dplyr::recode,
                ctry = "Country",
                quarter = "Quarter",
                semester = "Semester",
                month = "Month",
                comparison = "Difference",
                trend = "Trend") |>
  dplyr::mutate(prop_prev = as.numeric(stringr::str_extract(!!dplyr::sym(names(prop_60)[length(names(prop_60)) - 1]),
                                                            regex_exp)),
                prop_cur = as.numeric(stringr::str_extract(!!dplyr::sym(names(prop_60)[length(names(prop_60))]),
                                                           regex_exp)),
                Quarter = factor(Quarter, ordered = TRUE)) |>
  dplyr::select(Country, Quarter, 6:9) |>
  SharedData$new()

filter_select("country-afp-prop-60", "Country: ", shared_prop_60,
              ~Country, 
              multiple = FALSE,
              allLevels = FALSE)

plot_ly(shared_prop_60, colors = c("lightsteelblue", "steelblue")) |>
    add_trace(
    x = ~Quarter,
    y = ~prop_prev,
    type = "bar",
    text = ~get(names(prop_60)[6]),
    textposition = 'outside',
    name = names(prop_60)[length(names(prop_60)) - 1],
    color = as.factor(names(prop_60)[length(names(prop_60)) - 1])
  ) |>
  add_trace(
    x = ~Quarter,
    y = ~prop_cur,
    type = "bar",
    text = ~get(names(prop_60)[7]),
    textposition = 'outside',
    name = names(prop_60)[length(names(prop_60))],
    color = as.factor(names(prop_60)[length(names(prop_60))])
  ) |>
  config(displayModeBar = FALSE) |>
  layout(title = list(
    text = paste0("<b>Proportion of 60 Days Follow-Up Done</b>"),
    x = 0,
    xanchor = "left"),
    yaxis = list(title = "Proportion with Follow Up"),
    xaxis = list(title = "Quarter"),
    legend = list(orientation = "h",
                           xanchor = "center", 
                           x = 0.5,
                           y = -0.2),
    margin = list(t = 50)
         )
```

### Proportion of lab pending

Shows the proportion of lab samples not yet received in the culturing lab, with pending classification.

```{r afp-prop-lab-pending}
#| echo: false
#| warning: false

lab_pending_with_prop <- lab_pending |>
  dplyr::mutate(prop = as.numeric(stringr::str_extract(prop_label, "[0-9]+")))

shared_lab_pending <- lab_pending_with_prop |>
  dplyr::rename(
      Region = "whoregion",
      Country = "country") |>
  dplyr::arrange(dplyr::desc(prop)) |>
  SharedData$new(key = ~Region)

filter_select("region-afp-lab-pending", "Region: ", shared_lab_pending,
              ~Region, 
              multiple = FALSE,
              allLevels = FALSE)

plot_ly(shared_lab_pending) |>
  add_trace(
    x = ~Country,
    y = ~prop,
    type = "bar",
    name = "Proportion of Samples Older Than 90 Days with Pending Status"
  ) |>
  config(displayModeBar = FALSE) |>
  layout(title = list(
    text = paste0("<b>AFP Lab Samples Older Than 90 Days With Pending Status</b>"),
    x = 0,
    xanchor = "left"),
    yaxis = list(title = "% Pending Samples"),
    xaxis = list(title = "Country"),
    margin = list(t = 50)
         )
```

### Proportion of cases classified
```{r afp-prop-cases-classified}
#| echo: false
#| warning: false

# Graph
shared_prop_classified <- prop_classified |>
    dplyr::rename(
      Country = "ctry",
      Quarter = "quarter",
      `% Difference` = "diff",
      Trend = "trend",
      `Current Year Performance` = "performance"
    ) |>
  dplyr::mutate(prop_prev = stringr::str_extract(get(names(prop_classified)[4]), "[0-9]+|NaN"),
                prop_cur = stringr::str_extract(get(names(prop_classified)[5]), "[0-9]+|NaN")
                ) |>
  dplyr::mutate(prop_cur = as.numeric(prop_cur),
                prop_prev = as.numeric(prop_prev)) |>
  dplyr::select(1,3:5, 9:10) |>
  SharedData$new()

filter_select("country-afp-prop-classified", "Country: ", shared_prop_classified,
              ~Country, 
              multiple = FALSE,
              allLevels = FALSE)

plot_ly(shared_prop_classified, colors = c("lightsteelblue", "steelblue")) |>
    add_trace(
    x = ~Quarter,
    y = ~prop_prev,
    type = "bar",
    text = ~get(names(prop_classified)[4]),
    textposition = 'outside',
    name = names(prop_classified)[4],
    color = as.factor(names(prop_classified)[4])
  ) |>
  add_trace(
    x = ~Quarter,
    y = ~prop_cur,
    type = "bar",
    text = ~get(names(prop_classified)[5]),
    textposition = 'outside',
    name = names(prop_classified)[5],
    color = as.factor(names(prop_60)[5])
  ) |>
  config(displayModeBar = FALSE) |>
  layout(title = list(
    text = paste0("<b>Proportion of Cases Classified</b>"),
    x = 0,
    xanchor = "left"),
    yaxis = list(title = "Proportion Classified"),
    xaxis = list(title = "Quarter"),
    legend = list(orientation = "h",
                           xanchor = "center", 
                           x = 0.5,
                           y = -0.2),
    margin = list(t = 50)
         )

```

### Timeliness of detection for WPV/VDPV 

```{r afp-det-wpv-vdpv}
#| echo: false
#| warning: false

# Graph
shared_afp_wpv_vdpv <- afp_wpv_vdpv |>
  SharedData$new()

filter_select("country-afp-wpv-vdpv", "Country: ", shared_afp_wpv_vdpv,
              ~country, 
              multiple = FALSE,
              allLevels = FALSE)

plot_ly(shared_afp_wpv_vdpv, colors = c("lightsteelblue", "steelblue")) |>
    add_trace(
    x = ~month,
    y = ~`2024`,
    type = "bar",
    name = "2024",
    color = as.factor("2024")
  ) |>
  add_trace(
    x = ~month,
    y = ~`2025`,
    type = "bar",
    name = "2025",
    color = as.factor("2025")
  ) |>
  config(displayModeBar = FALSE) |>
  layout(title = list(
    text = paste0("<b>Timeliness of Detection for AFP WPV/VDPV Samples</b>"),
    x = 0,
    xanchor = "left"),
    yaxis = list(title = "Median Days"),
    xaxis = list(title = ""),
    legend = list(orientation = "h",
                           xanchor = "center", 
                           x = 0.5,
                           y = -0.2),
    margin = list(t = 50)
         )
```
:::

:::

## Detailed Tables
:::panel-tabset

## AFP Surveillance Indicators
::: panel-sidebar
### Number of AFP cases reported

```{r afp-case-reported-table}
#| echo: false

datatable(
  afp_cases_reported |>
    dplyr::rename(
      Region = "whoregion",
      Country = "place.admin.0",
      Month = "month"
    ),
  filter = "top",
  extensions = 'Buttons',
  options = list(
    dom = 'Bfrtip',
    buttons = c('copy', 'csv', 'excel'),
    pageLength = 10
  )
)
```

### Proportion of 60 days follow-up done
```{r afp-prop-60-table}
#| echo: false

datatable(
  prop_60 |>
    dplyr::rename_with(dplyr::recode,
                ctry = "Country",
                quarter = "Quarter",
                semester = "Semester",
                month = "Month",
                comparison = "Difference",
                trend = "Trend"),
  filter = "top",
  extensions = 'Buttons',
  options = list(
    dom = 'Bfrtip',
    buttons = c('copy', 'csv', 'excel'),
    pageLength = 10
  )
)
```

### Proportion of lab pending

Shows the proportion of lab samples not yet received in the culturing lab, with pending classification.
```{r afp-prop-lab-pending-table}
#| echo: false

datatable(
  lab_pending |>
    dplyr::rename(
      Country = "country",
      Region = "whoregion",
      `Pending Samples` = "pending_samples",
      `% Lab Samples` = "prop_label"
    ),
  filter = "top",
  extensions = 'Buttons',
  options = list(
    dom = 'Bfrtip',
    buttons = c('copy', 'csv', 'excel'),
    pageLength = 10
  )
)

```

### Proportion of cases classified
Proportion of cases with inadequate samples classified by the ERC.
```{r afp-prop-case-classified-table}
#| echo: false

datatable(
  prop_classified |>
    dplyr::rename(
      Country = "ctry",
      Quarter = "quarter",
      `% Difference` = "diff",
      Trend = "trend",
      `Current Year Performance` = "performance"
    ),
  filter = "top",
  extensions = 'Buttons',
  options = list(
    dom = 'Bfrtip',
    buttons = c('copy', 'csv', 'excel'),
    pageLength = 10
  )
)
```

### Timeliness of detection for WPV/VDPV samples
```{r afp-timely-wpv-vpdv-table}
#| echo: false

datatable(
  afp_wpv_vdpv |>
    dplyr::rename_with(recode,
                country = "Country",
                whoregion = "Region",
                month = "Month",
                quarter = "Quarter",
                comparison = "Difference",
                trend = "Trend"),
  filter = "top",
  extensions = 'Buttons',
  options = list(
    dom = 'Bfrtip',
    buttons = c('copy', 'csv', 'excel'),
    pageLength = 10
  )
)

```

### Timeliness of detection for negative samples
From date of collection to final cell culture results.
```{r afp-timely-neg-samples-table}
#| echo: false

datatable(
  negative_lab_processing |>
    dplyr::rename(
      Country = "country",
      Region = "whoregion",
      Category = "culture.itd.cat",
      Month = "month",
      Difference = "comparison",
      Trend = "trend"
    ),
  filter = "top",
  extensions = 'Buttons',
  options = list(
    dom = 'Bfrtip',
    buttons = c('copy', 'csv', 'excel'),
    pageLength = 10
  )
)

```

### Timeliness of stool specimen shipment
```{r afp-stool-ship-table}
#| echo: false

datatable(
  afp_shipment_timeliness |>
    dplyr::rename_with(recode,
      country = "Country",
      whoregion = "Region",
      month = "Month",
      comparison = "Difference",
      culture.itd.cat = "Shipment Type",
      trend = "Trend"
    ),
  filter = "top",
  extensions = 'Buttons',
  options = list(
    dom = 'Bfrtip',
    buttons = c('copy', 'csv', 'excel'),
    pageLength = 10
  )
)

```

### Timeliness of lab processing of AFP samples
`r max_date_notif`

```{r afp-timely-lab-process-table}
#| echo: false

datatable(
  afp_lab_processing |>
    dplyr::rename_with(recode,
      country = "Country",
      month = "Month",
      quarter = "Quarter",
      whoregion = "Region",
      comparison = "Difference",
      trend = "Trend"
    ),
  filter = "top",
  extensions = 'Buttons',
  options = list(
    dom = 'Bfrtip',
    buttons = c('copy', 'csv', 'excel'),
    pageLength = 10
  )
)
```

### Proportion of inadequates with contact sampling
```{r afp-prop-inad-table}
#| echo: false

datatable(
  prop_inad |>
    dplyr::rename(
      Country = "ctry",
      Quarter = "quarter",
      Region = "whoregion",
      `Difference, %` = "comparison",
      Trend = "trend"
    ),
  filter = "top",
  extensions = 'Buttons',
  options = list(
    dom = 'Bfrtip',
    buttons = c('copy', 'csv', 'excel'),
    pageLength = 10
  )
)

```
:::

## Environmental Surveillance Indicators
::: panel-sidebar
### Timeliness of ES sample shipment

```{r}
#| label: es-lab-shipment
#| echo: false
es_shipment <- es_shipment |>
  dplyr::rename(
    Month = "month",
    Country = "country",
    `Lab Type` = "es.lab.type",
    Region = "who.region",
    Difference = "diff",
    Trend = "trend",
    `Current Year Timeliness` = "current_year_timeliness",
    `Trend Summary` = "trend_summary"
  )

datatable(
  es_shipment,
  filter = "top",
  extensions = 'Buttons',
  options = list(
    dom = 'Bfrtip',
    buttons = c('copy', 'csv', 'excel'),
    pageLength = 10
  )
)

```

### Timeliness of detection for WPV/VDPV-ES

```{r}
#| label: es-wpv-vdpv
#| echo: false

es_wpv_vdpv <- es_wpv_vdpv |>
  dplyr::rename(
    Month = "month",
    Country = "country",
    `Lab Type` = "es.lab.type",
    Region = "who.region",
    Difference = "diff",
    Trend = "trend",
    `Current Year Timeliness` = "current_year_timeliness",
    `Trend Summary` = "trend_summary"
  )

datatable(
  es_wpv_vdpv,
  filter = "top",
  extensions = 'Buttons',
  options = list(
    dom = 'Bfrtip',
    buttons = c('copy', 'csv', 'excel'),
    pageLength = 10
  )
)
```

### Number of operational ES sites per country

```{r}
#| label: es-sites
#| include: false
#| echo: false

es_sites <- es_sites |>
  dplyr::rename(
    Country = "ctry",
    Difference = "comparison",
    `Difference, %` = "prop_diff",
    Trend = "trend"
  )
```

```{r}
#| include: true
#| echo: false

datatable(
  es_sites,
  filter = "top",
  extensions = 'Buttons',
  options = list(
    dom = 'Bfrtip',
    buttons = c('copy', 'csv', 'excel'),
    pageLength = 10
  )
)

```
### Number of samples per sites

```{r}
#| label: es-site-samples
#| include: false
#| echo: false

failing_sites <- es_site_samples |>
  dplyr::filter(stringr::str_detect(failing_performance, "Yes \\(")) |>
  dplyr::group_by(ADM0_NAME) |>
  dplyr::summarize(n = dplyr::n()) |>
  dplyr::arrange(dplyr::desc(ADM0_NAME))

failing_active_sites <- es_site_samples |>
  dplyr::filter(stringr::str_detect(failing_performance, "Yes \\("),
                active_site == "Yes") |>
  dplyr::group_by(ADM0_NAME) |>
  dplyr::summarize(n = dplyr::n()) |>
  dplyr::arrange(dplyr::desc(ADM0_NAME))

es_failure_summary <- paste0(
  "There are ", nrow(failing_sites), " countries with sites below target, ",
  "with a total of ", sum(failing_sites$n, na.rm = TRUE), " sites.",
  " Failing sites are those who have been open for at least 12 months", 
  " with no collections in the last two months or",
  " have less than 10 samples in the past rolling 12-month period. Of the sites below target, ",
  sum(failing_active_sites$n, na.rm = TRUE),
  " are considered active sites (sites with at least 10 collections in the past rolling 12-month period), ",
  "with no EV detection in the past two months."
)

es_site_samples <- es_site_samples |>
  dplyr::rename(
    Country = "ADM0_NAME",
    `Site Name` = "site.name",
    `Site Age` = "site_age",
    `Samples In Last 12 Months` = "n_samples_12_mo",
    `POLIS Site Status` = "site.status",
    `Earliest Collection` = "earliest_collection",
    `Last Collection` = "last_collection",
    `Days Since Last Collection` = "days_since_last_collection",
    `Collection In Two Months` = "collection_two_mo",
    `Last Detection` = "last_detection",
    `Days Since Last Detection` = "days_since_last_det",
    `Detection In Two Months` = "detection_two_mo",
    `Is Active Site` = "active_site",
    `Below Target Performance` = "failing_performance"
  )

```
`r es_failure_summary`
```{r}
#| include: true
#| echo: false
datatable(
  es_site_samples,
  filter = "top",
  extensions = 'Buttons',
  options = list(
    dom = 'Bfrtip',
    buttons = c('copy', 'csv', 'excel'),
    pageLength = 10
  )
)

```

:::

## Lab Surveillance Indicators
`{r} max_date_notif`

::: panel-sidebar
### Timeliness of virus isolation results

```{r}
#| include: false
#| echo: false

culture_lab_intervals <- culture_lab_intervals |>
  dplyr::rename_with(recode,
                     culture.itd.lab = "Culture/ITD Lab",
                     comparison = "Difference",
                     prop_diff = "Difference, %",
                     trend = "Trend")

interval_summary <- culture_lab_intervals |> 
    dplyr::filter(interval == "days.lab.culture") |>
    dplyr::select(-interval)
top_10 <- interval_summary |> 
  filter(Trend == "Increase") |> 
  arrange(desc(Difference)) |>
  select(-Trend) |>
  slice_head(n = 10)
bottom_10 <-interval_summary |> 
  filter(Trend == "Decrease") |> 
  arrange(Difference) |>
  select(-Trend) |>
  slice_head(n = 10)

summary_string_top <- glue::glue_data(
  top_10,
  "{str_to_title(`Culture/ITD Lab`)} ({round(Difference, 1)})"
)
summary_string_bottom <- glue::glue_data(
  bottom_10,
  "{str_to_title(`Culture/ITD Lab`)} ({round(Difference, 1)})"
)

# Combine into a single sentence
final_summary_top <- paste0("The top labs with increased median timeliness compared to the three year median (difference in days shown) are: ", paste(summary_string_top, collapse = ", "), ".")
final_summary_bottom <- paste0("The top 10 labs with the biggest improvement in median timeliness compared to the three year median (difference in days shown) are: ", paste(summary_string_bottom, collapse = ", "), ".")
comparible <- paste("There were", interval_summary |> 
                      filter(Trend == "Same") |> 
                      nrow(), "labss the stayed in the same levels and", 
                    interval_summary |> 
                      filter(Trend == "No data available for both years") |> 
                      nrow(), "labs with missing 3-year medians or 2025 medians.")
overall <- paste0("Overall, ", 
                  interval_summary |> 
                    filter(!is.na(`Culture/ITD Lab`), 
                           Trend != "No data available for both years") |>
                    pull(`Culture/ITD Lab`) |> 
                    unique() |> 
                    length(), " labs had data in the lab dataset. ",
                  interval_summary |>
                    filter(!is.na(`Culture/ITD Lab`)) |>
                    select(4) |> 
                    filter(if_all(1, \(x) !is.na(x))) |>
                    nrow(), " labs had medians for the current year and ",
                  interval_summary |> 
                    filter(!is.na(`Culture/ITD Lab`)) |>
                    select(3) |> 
                    filter(if_all(1, \(x) !is.na(x))) |>
                    nrow(), " labs had 3-year medians."
                  )

```

#### Summary

`r final_summary_top`

`r final_summary_bottom`

`r comparible`

`r overall`

```{r}
#| include: true
#| echo: false

datatable(interval_summary,
  filter = "top",
  extensions = 'Buttons',
  options = list(
    dom = 'Bfrtip',
    buttons = c('copy', 'csv', 'excel'),
    pageLength = 10
  )
)
```

### Timeliness of ITD results

```{r}
#| include: false
#| echo: false

interval_summary <- culture_lab_intervals |> 
    dplyr::filter(interval == "days.culture.itd") |>
    dplyr::select(-interval)
top_10 <- interval_summary |> 
  filter(Trend == "Increase") |> 
  arrange(desc(Difference)) |>
  select(-Trend) |>
  slice_head(n = 10)
bottom_10 <-interval_summary |> 
  filter(Trend == "Decrease") |> 
  arrange(Difference) |>
  select(-Trend) |>
  slice_head(n = 10)

summary_string_top <- glue::glue_data(
  top_10,
  "{str_to_title(`Culture/ITD Lab`)} ({round(Difference, 1)})"
)
summary_string_bottom <- glue::glue_data(
  bottom_10,
  "{str_to_title(`Culture/ITD Lab`)} ({round(Difference, 1)})"
)

# Combine into a single sentence
final_summary_top <- paste0("The top labs with increased median timeliness compared to the three year median (difference in days shown) are: ", paste(summary_string_top, collapse = ", "), ".")
final_summary_bottom <- paste0("The top 10 labs with the biggest improvement in median timeliness compared to the three year median (difference in days shown) are: ", paste(summary_string_bottom, collapse = ", "), ".")
comparible <- paste("There were", interval_summary |> 
                      filter(Trend == "Same") |> 
                      nrow(), "labss the stayed in the same levels and", 
                    interval_summary |> 
                      filter(Trend == "No data available for both years") |> 
                      nrow(), "labs with missing 3-year medians or 2025 medians.")
overall <- paste0("Overall, ", 
                  interval_summary |> 
                    filter(!is.na(`Culture/ITD Lab`), 
                           Trend != "No data available for both years") |>
                    pull(`Culture/ITD Lab`) |> 
                    unique() |> 
                    length(), " labs had data in the lab dataset. ",
                  interval_summary |>
                    filter(!is.na(`Culture/ITD Lab`)) |>
                    select(4) |> 
                    filter(if_all(1, \(x) !is.na(x))) |>
                    nrow(), " labs had medians for the current year and ",
                  interval_summary |> 
                    filter(!is.na(`Culture/ITD Lab`)) |>
                    select(3) |> 
                    filter(if_all(1, \(x) !is.na(x))) |>
                    nrow(), " labs had 3-year medians."
                  )
```

#### Summary

`r final_summary_top`

`r final_summary_bottom`

`r comparible`

`r overall`

```{r}
#| include: true
#| echo: false

datatable(
  culture_lab_intervals |> 
    dplyr::filter(interval == "days.culture.itd") |>
    dplyr::select(-interval),
  filter = "top",
  extensions = 'Buttons',
  options = list(
    dom = 'Bfrtip',
    buttons = c('copy', 'csv', 'excel'),
    pageLength = 10
  )
)
```

### Timeliness of shipment for sequencing

```{r}
#| include: false
#| echo: false

interval_summary <- culture_lab_intervals |> 
    dplyr::filter(interval == "days.seq.ship") |>
    dplyr::select(-interval)
top_10 <- interval_summary |> 
  filter(Trend == "Increase") |> 
  arrange(desc(Difference)) |>
  select(-Trend) |>
  slice_head(n = 10)
bottom_10 <-interval_summary |> 
  filter(Trend == "Decrease") |> 
  arrange(Difference) |>
  select(-Trend) |>
  slice_head(n = 10)

summary_string_top <- glue::glue_data(
  top_10,
  "{str_to_title(`Culture/ITD Lab`)} ({round(Difference, 1)})"
)
summary_string_bottom <- glue::glue_data(
  bottom_10,
  "{str_to_title(`Culture/ITD Lab`)} ({round(Difference, 1)})"
)

# Combine into a single sentence
final_summary_top <- paste0("The top labs with increased median timeliness compared to the three year median (difference in days shown) are: ", paste(summary_string_top, collapse = ", "), ".")
final_summary_bottom <- paste0("The top 10 labs with the biggest improvement in median timeliness compared to the three year median (difference in days shown) are: ", paste(summary_string_bottom, collapse = ", "), ".")
comparible <- paste("There were", interval_summary |> 
                      filter(Trend == "Same") |> 
                      nrow(), "labss the stayed in the same levels and", 
                    interval_summary |> 
                      filter(Trend == "No data available for both years") |> 
                      nrow(), "labs with missing 3-year medians or 2025 medians.")
overall <- paste0("Overall, ", 
                  interval_summary |> 
                    filter(!is.na(`Culture/ITD Lab`), 
                           Trend != "No data available for both years") |>
                    pull(`Culture/ITD Lab`) |> 
                    unique() |> 
                    length(), " labs had data in the lab dataset. ",
                  interval_summary |>
                    filter(!is.na(`Culture/ITD Lab`)) |>
                    select(4) |> 
                    filter(if_all(1, \(x) !is.na(x))) |>
                    nrow(), " labs had medians for the current year and ",
                  interval_summary |> 
                    filter(!is.na(`Culture/ITD Lab`)) |>
                    select(3) |> 
                    filter(if_all(1, \(x) !is.na(x))) |>
                    nrow(), " labs had 3-year medians."
                  )

```

#### Summary

`r final_summary_top`

`r final_summary_bottom`

`r comparible`

`r overall`

```{r}
#| include: true
#| echo: false

datatable(
  culture_lab_intervals |> 
    dplyr::filter(interval == "days.seq.ship") |>
    dplyr::select(-interval),
  filter = "top",
  extensions = 'Buttons',
  options = list(
    dom = 'Bfrtip',
    buttons = c('copy', 'csv', 'excel'),
    pageLength = 10
  )
)
```

### Timeliness of sequencing results

```{r}
#| include: false
#| echo: false
seq_lab_interval <- seq_lab_interval |>
  dplyr::rename_with(recode,
                     seq.lab = "Sequencing Lab",
                     comparison = "Difference",
                     prop_diff = "Difference, %",
                     trend = "Trend")

interval_summary <- seq_lab_interval |> 
    dplyr::filter(interval == "days.seq.rec.res") |>
    dplyr::select(-interval)
top_10 <- interval_summary |> 
  filter(Trend == "Increase") |> 
  arrange(desc(Difference)) |>
  select(-Trend) |>
  slice_head(n = 10)
bottom_10 <-interval_summary |> 
  filter(Trend == "Decrease") |> 
  arrange(Difference) |>
  select(-Trend) |>
  slice_head(n = 10)

summary_string_top <- glue::glue_data(
  top_10,
  "{str_to_title(`Sequencing Lab`)} ({round(Difference, 1)})"
)
summary_string_bottom <- glue::glue_data(
  bottom_10,
  "{str_to_title(`Sequencing Lab`)} ({round(Difference, 1)})"
)

# Combine into a single sentence
final_summary_top <- paste0("The top labs with increased median timeliness compared to the three year median (difference in days shown) are: ", paste(summary_string_top, collapse = ", "), ".")
final_summary_bottom <- paste0("The top 10 labs with the biggest improvement in median timeliness compared to the three year median (difference in days shown) are: ", paste(summary_string_bottom, collapse = ", "), ".")
comparible <- paste("There were", interval_summary |> 
                      filter(Trend == "Same") |> 
                      nrow(), "labss the stayed in the same levels and", 
                    interval_summary |> 
                      filter(Trend == "No data available for both years") |> 
                      nrow(), "labs with missing 3-year medians or 2025 medians.")
overall <- paste0("Overall, ", 
                  interval_summary |> 
                    filter(!is.na(`Sequencing Lab`), 
                           Trend != "No data available for both years") |>
                    pull(`Sequencing Lab`) |> 
                    unique() |> 
                    length(), " labs had data in the lab dataset. ",
                  interval_summary |>
                    filter(!is.na(`Sequencing Lab`)) |>
                    select(4) |> 
                    filter(if_all(1, \(x) !is.na(x))) |>
                    nrow(), " labs had medians for the current year and ",
                  interval_summary |> 
                    filter(!is.na(`Sequencing Lab`)) |>
                    select(3) |> 
                    filter(if_all(1, \(x) !is.na(x))) |>
                    nrow(), " labs had 3-year medians."
                  )

```

#### Summary

`r final_summary_top`

`r final_summary_bottom`

`r comparible`

`r overall`

```{r}
#| include: true
#| echo: false

datatable(
  seq_lab_interval |> 
    dplyr::filter(interval == "days.seq.rec.res") |>
    dplyr::select(-interval),
  filter = "top",
  extensions = 'Buttons',
  options = list(
    dom = 'Bfrtip',
    buttons = c('copy', 'csv', 'excel'),
    pageLength = 10
  )
)
```

### Laboratory workload

```{r}
#| include: false
#| echo: false

lab_workload <- lab_workload |>
  dplyr::rename(
    `Culture/ITD Lab` = "culture.itd.lab",
    Month = "month",
    `Countries Served` = "from_ctry",
    Difference = "comparison",
    `Difference (%)` = "comparison_pct",
    Trend = "trend"
  )

last_month <- lab_workload |>
  filter(Month == lubridate::month(lubridate::month(max_lab_date) - 1, TRUE))

top_10 <- last_month |>
  filter(Trend == "Increase") |>
  arrange(desc(Difference)) |>
  slice_head(n = 10)

bottom_10 <- last_month |> 
  filter(Trend == "Decrease") |> 
  arrange(Difference) |>
  select(-Trend) |>
  slice_head(n = 10)

summary_string_top <- glue::glue_data(
  top_10,
  "{str_to_title(`Culture/ITD Lab`)} ({round(Difference, 1)})"
)
summary_string_bottom <- glue::glue_data(
  bottom_10,
  "{str_to_title(`Culture/ITD Lab`)} ({round(Difference, 1)})"
)

# Combine into a single sentence
final_summary_top <- paste0("The top 10 labs with increased lab samples compared to the previous year (difference in samples shown) are: ", paste(summary_string_top, collapse = ", "), ".")
final_summary_bottom <- paste0("The top 10 labs with reduced lab samples compared to the previous year (difference in samples shown) are: ", paste(summary_string_bottom, collapse = ", "), ".")
comparible <- paste("There were", last_month |> 
                      filter(Trend == "Same") |> nrow(), "labs the stayed in the same levels and", last_month |> 
                      filter(Trend == "No data available for both years") |> 
                      nrow(), "labs with missing data for the current year and previous year.")
overall <- paste0("Overall, ", 
                  last_month |> 
                    filter(if_any(5, \(x) !is.na(x))) |> 
                    nrow(), 
                  " labs had data from the previous month of the current year using the latest month available in the lab dataset.")

```

#### Summary

**For the previous month from the latest date available in the lab dataset** 
*`r max_date_notif`*

`r final_summary_top` 

`r final_summary_bottom` 

`r comparible`

`r overall`

```{r}
#| include: true
#| echo: false
datatable(
  lab_workload,
  filter = "top",
  extensions = 'Buttons',
  options = list(
    dom = 'Bfrtip',
    buttons = c('copy', 'csv', 'excel'),
    pageLength = 10
  )
)
```
:::

:::
